import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    root: {
        marginTop: 80,
    },
    textField: {
        marginTop: 20,
    },
    button: {
        marginBottom: 20,
        marginTop: 10,
        
    },
    profPic: {
        marginTop: 20,
        marginBottom: 30
    }
})

export default useStyles