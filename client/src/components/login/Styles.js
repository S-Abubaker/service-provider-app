import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    root: {
        marginTop: 80,
    },
    about: {
        marginBottom: 40
    },
    textField: {
        marginBottom: 20,
    },
    button: {
        marginBottom: 20,
        marginTop: 10,
        
    },
})

export default useStyles